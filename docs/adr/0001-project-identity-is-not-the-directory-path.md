# Project identity is not the directory path

Projects use a stable project ID instead of using the project directory path as identity. Directory paths can be renamed, moved, or represented differently across runners, while sessions and history should remain attached to the same project; the path is project metadata, not the project identity.
