# Project directory can change without moving sessions

A project's directory path may change while the project ID and existing session ownership remain unchanged. This supports moved or renamed folders without rewriting history; new sessions use the current project directory, while existing sessions remain attached to the project that created them.
